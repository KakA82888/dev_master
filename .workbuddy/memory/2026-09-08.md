# 2026-09-08（D2）工作日志

## PM 进度汇报（关德豪）
- 产出 `docs/00_项目计划/进度汇报_D2_2026-09-08.md`：计划 vs 实际、三项硬指标、任务书十项交付物对照、4 项待拍板、D3–D6 分角色派工、风险 R1–R6。

## 实测结论（本次新发现，后续会话可直接引用）
- `python -m pytest -q` → **23 passed in 6.55s**，工作区干净。
- Git：9 次提交，最新 f53078c；**提交身份仍为占位 `ReportAgentDev <dev@example.com>`**。
- **无 `.env`**（只有 .env.example）→ LLM 双通道未启用，当前全程规则通道。
- `prompts/{schema,system,templates}` **三个子目录全空** → 系统提示词、JSON schema、报告模板库未落盘（任务书第 3、4 项交付缺口）。
- `corpus/`、`deliver/` **空目录** → 语料库与交付包 0 进度。
- `data/eval/` 只有 `reference_auto.json` → **评测集 golden 与 eval 脚本全无**，SM2（解析 ≥90%）未测量，是当前关键路径第一棒（需用户本人标注 1–2h）。
- **基线数字不一致**：《每日执行清单》D2 写"有效订单 697 / 客单价 424.80"，但 `scripts/metrics/test_metrics.py` 实测 691 / 428.49（转化率 691/854）。需裁定以代码实测为准并回改清单，否则答辩被问会翻车。

## S6 评测体系完成（高级开发工程师）
- 评测集：主集 `data/eval/eval_set_golden.csv` 55 条 + 留出集 `eval_set_holdout.csv` 35 条（四维矩阵：类型×时间×市场×边界/歧义/注入）。
- 脚本：`scripts/eval/{build_eval_template,eval_parse,eval_metrics,test_eval}.py`。
- **解析准确率 70.91% → 100%**（主集 55/55，留出集 35/35），指标三方一致差异 0，端到端均值 0.112s。
- 修 4 处解析 bug：D01/D04 整月规则抢先于绝对日期；G04 省略"日号"+裸数字右端；G06 显式类型与单日冲突未扩展。新增 `_expand_to_period` / `_has_explicit_day` / 步骤 0。
- 修崩溃：`InvalidDateError` + `_mkdate`/`month_range` 校验（"2011年13月"曾直接抛 ValueError）。
- **新建 `scripts/agent/guard.py` 安全网关**（原本规则通道零拒绝机制，违反任务书 6.2 / PRD R11）：原因码 injection/overreach/fabrication/unsupported/out_of_range/invalid_date；已接入 `orchestrator._plan`（改调 `parse_safe`，拦截即返回 error 不出报）。
- `pytest -q` → **26 passed**（原 23 + 新增 3 条评测回归断言：主集≥90%、留出集≥90%、reject 全拦截）。
- 报告：`docs/06_测试报告_v0.2_S6评测.md`。

## 遗留风险（S6）
- **golden 由 AI 预填，未经业务负责人复核**——100% 属"自己出题自己判"，已用留出集验证泛化，仍需人工抽查歧义类（G02/G04/G06）与边界类（F01–F05 该拒绝还是出空报）。
- 规则通道天花板：接 LLM 后需重跑本评测集对比两通道分数。

## 集成测试（WebappTestingExpert，第二轮）
- 全栈实测通过：pytest 26 passed；SM1 三方差异 0.000000、SM2 100%（主集+留出集）、SM3/SM5 性能达标；后端 HTTP 冒烟 15/15（health/auth/生成/确认/导出/跨用户 404）；前端 `tsc --noEmit` 0 error、资源经 :8000 正常托管（JS 164KB / CSS 12KB）、API 用相对路径无端口错配。
- **修复 1 个安全纵深漏洞**：`guard.py` 原先缺破坏性 SQL 关键词规则，指令 `生成昨日日报; DROP TABLE sales_detail;--` 未被拦截（但数据层参数化查询保证不会真删表，已核 sales_detail 仍 1,033,031 行）。新增 `sqli` 规则后五类危险指令（注入/越权/伪造/越界/SQLi）全部拦截，正常指令放行。
- **注意**：改 `guard.py` 后必须重启 uvicorn（模块缓存），首轮线上复测失败即因测的是旧进程。
- 清理了测试污染：删 secuser*/smoke* 测试用户与报告，仅保留预置 `demo` 用户；备份 `data/processed/app.db.bak_test_*`。
- 新增 `scripts/eval/smoke_http.py`（幂等 HTTP 冒烟脚本，用户名带随机后缀可重复跑）。
- 报告：`docs/06_集成测试报告_2026-09-08.md`。

## 进度定调
- 代码层（S0–S5）已于 09-07 全部完成，较计划提前 5–6 天，M1 实质达成。
- 未启动/半成品：S6 评测（0）、S7 部署（无手册）、S8 安全自查（无）、S9 文档后三项（部署手册/AI协作教程/语料库/PPT/参数文档全 0）。
- 待用户拍板：① golden 标注 ② LLM Key ③ 数据源天池→UCI 变更确认 ④ Git 身份。

## D8 LLM 通道接入（用户于 10:27 提供 SiliconFlow Key 后）
- **Key/模型**：SiliconFlow `Pro/deepseek-ai/DeepSeek-R1`，base_url `https://api.siliconflow.cn/v1`；Key 已写入 `.env`（gitignore 已含 .env，不入库）。
- **新增 `scripts/agent/llm_client.py`**：OpenAI 兼容客户端封装，`llm_parse_intent(instruction, anchor)` 调大模型做意图解析，输出 JSON→Intent；硬约束：LLM 只做 NLU，不生成任何金额数字；失败/未配置向上抛异常由编排层回退规则通道。
- **orchestrator 双通道改造**：`_plan` 现**先跑 guard 安全网关**（确定性拦截注入/越权/伪造/越界/SQLi），放行后才选通道；`LLM_MODE=llm` 且有 Key 时走 LLM 解析，否则/失败回退规则（原实现 guard 只在规则通道跑，已修正）。
- `main.py` 顶部加 `load_dotenv`；`.env` 含 LLM 块（LLM_MODE=llm 等）。
- 单挑冒烟 4 条全对（昨日日报/上周英国周报/2011-11-22 日报/2010-11 月报），延迟 9–52s（R1 推理慢，仍在 SM3 ≤2min 内）。
- **D8 对比评测完成**：`scripts/eval/eval_llm.py` 结果 = reject 安全不变量 12/12（LLM 模式下 guard 仍真实拦截）、normal 抽样 15 条 LLM 通道 **15/15=100%** 且规则通道 15/15=100%、全链路出报 OK、真实 HTTP 端到端（`生成上月德国月报`）HTTP 200 / drafted / 14.2s / 2235 字符含 GMV。报告 `docs/06_D8_LLM通道接入与评测.md`。
- **踩坑修复（重要）**：`llm_client` 在导入时 `load_dotenv` 会把 `LLM_MODE=llm` 注入整个进程，导致 pytest 后端生成类测试去调真实 LLM 而失败。已把 `.env` 加载移出模块级，改由入口（main.py / eval 脚本）显式 `load_dotenv`；并在 `conftest.py` 顶部强制 `os.environ["LLM_MODE"]="rule"` 做测试隔离。`pytest -q` → **26 passed**（恢复原状）。
- 待办：后台评测出数后更新集成测试报告 + 进度总报；并提醒用户 Key 已在对话明文出现、建议到控制台轮换。

## PM 文件审计（14:40 本轮，对照任务书 V1.0.0）
- 新增交付物 `docs/00_项目计划/进度汇报与文件审计_2026-09-08.md`：进度核对 + 三大 SM + 下一步计划 + 文件错误/疑问审计 + 待拍板 3 项。
- **实测复验**：`pytest -q` → **26 passed in 6.48s**（含 D8 未提交代码，仍全绿）。
- **本轮新发现的错误/疑问（已写入审计报告 §5）**：
  - E1 `docs/07_进度审核与交付文件总览.md` 过时：标 S0–S5、写「LLM Key 未接/S6 待评测」，与 09-08 实测 S6✅+D8✅ 不符。
  - E2 **D8 代码未入库**：git 工作区改动（main.py/conftest.py/intent_parser.py/orchestrator.py + eval_llm.py + data/eval/*）未提交，最新 commit 停 f53078c。
  - E3 `prompts/`、`corpus/`、`deliver/` 全空（任务书 5.2 硬交付缺口）。
  - E4 数据源天池→UCI 变更未书面化（任务书 3.3）；E5 Git 占位身份；E6 定时/批量任务未实现（仅 schema 预留）；E7 数据库加密/HTTPS 待部署补；E8 前端未真机回归+拦截提示不清；E9 golden 自验偏差；E11 两份 09-08 状态文档重叠待合并。
- 仍待用户拍板：D1 golden 抽查、D3 数据源变更确认、D4 Git 真实身份。

## P0 执行（14:59 本轮，用户指令「进行下一步」）
- **E1 已修**：`docs/07_进度审核与交付文件总览.md` 顶部加状态补记（标注 S0–S5 快照过期、指向 S6/D8 最新文档）。
- **E4/D3 已书面化**：新建 `docs/数据源变更说明.md`（DS-2026-001：天池→UCI Online Retail II 变更理由/决策记录(2026-09-07 G1门确认)/影响评估/同步文档清单/签署栏）。
- **E2 已入库**：两次提交，工作树干净——
  - `82ba452` feat(S6+D8)：llm_client/guard(sqli)/90条评测集/eval 脚本，19 files。
  - `2536a9a` docs：数据源变更说明 + 07 补记 + 三份 09-08 报告 + PM 审计交付物，12 files。
- 仍开放（需用户）：Git 真实身份（占位 ReportAgentDev，可 `--amend 82ba452` 或后续提交改身份）、Golden 抽查 15 条、模型换 V3（可选）。
