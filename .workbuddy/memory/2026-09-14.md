# 2026-09-14（D8）工作日志

## 项目进度汇报（用户要求"总结汇报当前项目进度"）
- **方法**：独立实测核查（不引用既有文档数字）——git log/status、pytest、交付包文件树逐目录 ls、.env 配置、实训截图位计数、后端 health。
- **实测结果**：
  - `pytest -q` → **30 passed** in 15.97s（较 09-10 的 26 增 4 条，为「X月第N周」回归用例）
  - git：**24 次提交**，工作树干净，最新 `70a1a41`
  - `.env`：`LLM_MODE=llm` / `deepseek-ai/DeepSeek-V4-Flash` / `LLM_TIMEOUT=20`
  - 交付包 7 大项齐备：01.code / 02.data(5) / 03.ppt(1, 52KB, 12页) / 04.实训指导书(9 文档, **31 处截图位待补**) / 05.RAG语料(19) / 【参数】文档 / 部署版code(已同构)
  - `deliver/` 尚未压缩为 zip
  - 后端 8000 当前**未运行**；前端 `app/frontend/dist` 存在（09-10 构建）
- **产出**：`docs/00_项目计划/进度汇报_D8_2026-09-14.md`（结论速览 / 排期对账 / 4 项硬验收指标 / 交付物盘点 / 实测证据 / 剩余工作 A-B-C-D 分类 / 风险 / 角色分工矩阵）。
- **结论**：S0–S10 全部落地，整体完成度约 90%，超前排期约 2 周；4 项硬指标 3 项达标（≥99% / 100% / 3–6s），浏览器兼容待真机走查。
- **剩余 bottleneck 在人工核验项（非开发）**：A1 Golden 抽查(~20min)、A2 Git 真实身份、A3 两项表述收紧（性能采样口径 / 浮点差异表述）；B1 前端拦截提示文案、B2 实训截图 31 张、B3 交付包压缩。
- **建议顺序**：A1 → A3 → B1 → B2 → B3，可在 1–2 工作日内推进至 100% 可交付。
- 未改动任何既有文件；未新增 git 提交（仅新增汇报文档，属 docs/00_项目计划，该目录已有同类文档，核查报告曾指出冗余、建议后续归档合并）。

## 全量代码审核（用户要求"严格审核所有开发步骤和代码，有则修改"）
- 方法：逐文件通读 15 个源文件（后端 9 + 指标 3 + agent 6 + 前端 4 关键）+ 独立实测复现，不采信既有文档结论。
- **发现 15 项问题，修复 14 项**：
  - **P0-1** `query_executor` 逐日明细未按 country 过滤 → 德国周报核心 £4,688.88 vs 明细合计 £309,378.44（**差 66 倍**，报告内部自相矛盾）。已修：country 非空时从事实表按国家实时聚合，全市场仍走 daily_agg 快路径。
  - P1-2 LLM 通道结果未经范围/市场校验（可产出超范围空报告、幻觉市场名静默 GMV=0）→ 新增 `_validate_llm_intent`（日期范围 + 37 国白名单），不通过则回退规则通道。
  - P1-3 提示词声明支持"今年/去年"但规则解析器不支持（回退时"去年"→"上个月"）→ 补齐整年/年月/年月日/月份区间四形态 + `_resolve_single_date` 加 `year_hint`。
  - P1-4 双轨对账**只覆盖单日**（`period_metrics_from_daily` 被 import 却未使用）→ 新增 4 个区间样本**三方对账**（daily_agg 累加 / 区间 SQL / pandas）+「跨天订单=0」前提断言；`reference_auto.json` 增 `feature_ranges` 字段（不改 `feature_days`，eval_metrics 兼容）。
  - P1-5 失败态无法区分「安全拦截」与「技术故障」（B1 根因）→ **error_code 全链路**：models 加列 + main 幂等 `ALTER TABLE` 迁移 + ReportSummary/ReportOut 双暴露 + 前端 `GATE_BLOCK_CODES`/`isGateBlocked`/`failureTitle` + 新增 `.alert.warn` 警示色。
  - P1-6 代码 `SUM(|amount|)`=1,462,797.75 vs 文档 `|SUM|`=1,462,050.61（差 747.14，来源：1 行 `is_refund=1 且 amount>0`）→ **以代码为准修文档**（理由：SUM(|amount|) 按天可加，是 daily_agg 前提；|SUM| 不可加）+ 新增「两种读法」说明段 + `indicators` docstring 强化。
  - P2×8：LLM 回退提示被返回值覆盖 / CORS 通配源+凭证 / JWT sub 非数字→500 / `?sync=true` 测试后门（加 `APP_ALLOW_SYNC` 开关，conftest 置 1）/ anomaly 阈值注释与区间口径不符 / guard 缺 `start>end` 与部分越界提示（新增 `range_warning`）/ guard 导入路径依赖 cwd / 输入无长度上限。
  - 观察×1（**未改**）：`detect_country` 子串匹配致「德国**整体**经营周报」→全市场；不改理由=可能造成已验证评测指标回退。
- **验证**：pytest 30 passed；cross_check（单日+区间）PASS；eval_parse 55/55=100%；eval_metrics PASS（均值 0.095s）；HTTP 端到端 injection/out_of_range 码正确落库、德国周报明细口径一致；tsc 0 错误 + vite build 成功；交付包 59 py 编译通过。
- 交付包已同步（`01.code` + `部署版code` + 04 实训语料口径文档副本）；**构建踩坑**：沙箱禁删导致 vite `emptyOutDir` 失败 → 改用 `vite build --no-emptyOutDir` 绕过（未改项目配置）。
- 报告：`docs/00_项目计划/代码与流程审核报告_2026-09-14.md`；07 总览已加 09-14 补记。
- **待用户拍板 3 项**：① 退款金额口径采纳方向（建议采纳代码口径）；② 是否收紧 ALL_MARKETS 匹配；③ 是否同步更新 PPT 第 09 页性能口径。

## GitHub 仓库推送（SSH: git@github.com:KakA82888/dev_master.git）
- 用户决策：① **项目作为仓库根**（覆盖远端占位 README）；② **移除 67MB 无关 zip 并清理历史**（本地文件承诺保留）。
- **勘察发现**：远端 `dev_master` 仅有 1 个提交 `8f6ee6f Initial commit` + 1 个占位 README；本地分支为 `master`、25 提交；`.git` 68M（几乎全是 `docs/AI共创面诊分析系统 (3).zip`，TP 家具参考模板，与电商项目无关）。
- **执行链路与踩坑**：
  1. 备份：`git bundle create D:/电商_repo_backup_20260914.bundle --all`（67MB，保留至今）。
  2. 历史重写：`git filter-branch --force --index-filter "git rm --cached --ignore-unmatch -- 'docs/*.zip'" --prune-empty -- --all`
     - **坑1**：首次失败 —— `Cannot rewrite branches: Your index contains uncommitted changes`（先前 `git rm --cached` 留下暂存删除）→ 先 `git reset -q` 复位索引再跑。
     - **坑2**：filter-branch 收尾会 `reset --hard`，**把工作树中不在新 HEAD 的文件一并删除**（zip 从磁盘消失，与"本地保留"承诺不符）→ 从备份恢复：
       `git fetch <bundle> refs/heads/master:refs/backup-tmp/master` → `git restore --source=refs/backup-tmp/master -- 'docs/*.zip'` → `git update-ref -d refs/backup-tmp/master`。
     - 用 **ASCII glob**（`'docs/*.zip'`）规避中文 pathspec 风险（记忆中的 git 中文路径事故）。
  3. 瘦身：`git reflog expire --expire=now --all` + `git gc --prune=now` → `.git` **68M → 1.2M**，历史中已无 zip 对象残留。
  4. `.gitignore` 新增 `docs/*.zip`、`.git-rewrite/`。
  5. 推送：`git branch -m master main` + `git push -u origin main --force`（forced update，覆盖占位 README）。
- **结果**：远端 `refs/heads/main = b803db3` = 本地 HEAD ✓；**241 文件 / 26 提交**；**无 `.env`、无 zip**；密钥扫描（sk-/api_key/Bearer）无命中。
- **沙箱限制**：无法创建 `refs/remotes/*` 引用（`git status` 恒显示 `[gone]`），不影响推送；`.git-rewrite` 需手动清理（已清）。
- 备份 bundle 保留在 `D:/电商_repo_backup_20260914.bundle`（含重写前完整历史，可随时回滚）。

## 任务书符合性对账（用户提供《电商运营报表自动化Agent项目任务书V1.0.0.docx》）
- **读取方式与踩坑**：按规范走 `tencent-docs-routing` → `tencent-local-office-edit`（editor_sdk）：
  - `open_file` 返回的 file_id 是**路径**；必须先用 `present_files` 打开，池中才会出现**真实 UUID** file_id（本次 `45fe731d-1010-4f73-aeca-57da88dce55f`）。
  - **editor_sdk 读不全**：`doc_resolve_document_structure` / `doc_get_outline` 的 `text_preview` 有**约 80 字符/段落、约 14 字符/单元格**的硬上限（设 `text_preview_length=200` 无效，idx 模式同样受限）。
  - **回退**：改用 `python-docx` 直接解析（**已向用户说明**），读出 72 段 + 2 表（4,932 字）。
- **对账结论**：核心交付项全部达成（指标 ≥99%、解析 100%、日报 3–6s、评测 90 条、Markdown+Word 导出、六类文档齐备、防注入 17/17）。
- **4 项硬缺口（任务书明文要求但未实现）**：
  1. **定时任务 + 批量任务**（§2.2 目标 1 + §3.2「定时调度器」）—— 仅 `schemas.ReportGenerateRequest` 留 `schedule`/`cron` 占位，无调度实现
  2. **修改意见沉淀 → 优化模板闭环**（§2.2 目标 4）—— 无 feedback 表/提交入口
  3. **浏览器兼容真机验证**（§6.4 明文验收）—— 未做
  4. **部署上线**（§3.3 第 5 阶段）—— 未实际部署
- **技术选型偏离**：前端**未使用 TailwindCSS / Ant Design**（package.json 仅 react + react-dom），实为自研 CSS 设计系统（tokens.css + styles.css 708 行）→ 建议出《技术选型调整说明》，而非改造。
- **合规待补（§6.2）**：数据库明文存储（要求加密）、HTTPS 未启用、无多角色 RBAC、无 LLM 输出敏感内容过滤。
- **文档符合性**：数据库设计（03 §2 数据模型）与 API 文档（03 §3 API 清单）合并于一份，需在索引中标注对应关系。
- **产出**：`docs/00_项目计划/任务书符合性对账与后续执行计划_2026-09-14.md`（差距 P0/P1/P2/P3 + A/B/C/D 四阶段计划 + 4 项待拍板）。

## 用户拍板与执行（11:00）
- **用户决策**：① 质疑 G1 出处 → 已给原文证据；② 前端选型「出说明」；③ 多角色；④ 无服务器，先本机部署。
- **G1 取证（文档内字符坐标，可跳转）**：
  - §2.2 目标 1（begin=839）：「…规划"数据读取→指标计算→波动分析→报告生成"任务链，**支持定时任务与批量任务**。」
  - §3.1 后端（begin=1382）：「负责用户认证、任务解析调度…，**定时任务**与长耗时任务采用异步处理。」
  - §3.2 技术表 R6（begin=1755 / 1769）：「外部工具/插件｜报表模板渲染组件、**定时调度器**｜报告填充导出与**定时任务**」
- **已完成 3 项**：
  1. **《技术选型调整说明》(TS-2026-001)** → `docs/技术选型调整说明.md`：自研 CSS 设计系统（tokens.css 78 + styles.css 708 + ui.tsx 146）替代 TailwindCSS/AntD 的理由（组件边界清晰 / 视觉一致性 / 产物 163.69kB JS + 12.04kB CSS / 规避重型依赖 / 与已有文档自洽）+ 影响评估 + 复现命令。
  2. **多角色 RBAC**：`User.role`(admin/user)；`_ensure_columns` 幂等补列且**新建该列时自动把既有种子账号提升为 admin**；新增 `/api/auth/me`；`GET /api/reports?scope=all` 管理员视图；`_get_owned` 允许管理员跨用户访问；前端角色徽章 +「全部用户报告」切换 + owner 展示；新增 4 条权限测试（越权列表 403 / 越权详情 404 / 管理员可读他人详情 / 默认仅本人）→ **pytest 34 passed**。
  3. **本机 HTTPS 部署**：`certs/` 自签证书（SAN=localhost,127.0.0.1；已加 .gitignore）；`start.sh`/`start.bat` 自动检测证书启用 TLS；部署手册新增 §5.1（含证书生成命令）并将 §9 的 HTTPS、角色权限两项状态转 ✅。
- **端到端验证**：HTTPS `/api/health` → 200；demo=admin、新注册=user；`scope=all` → 管理员 200 / 普通 403；普通用户默认列表仅见本人（0 条）。
- **交付包已同步**：01.code + 部署版code（代码 / dist / 启动脚本）+ 04 过程文档（部署手册 + 技术选型说明）。
- 提交 `720696b` 已推送 `origin/main`。
- **仍待处理**：**G1 定时/批量任务尚未实现**——原文证据已给出，等待用户确认是否纳入本轮。

## 待完成清单核查（11:08）
- 实测确认**仍未实现**：① 定时/批量任务 ② 修改意见沉淀→模板闭环 ③ 数据库存储加密 ④ LLM 输出敏感过滤 ⑤ 无《指标验证方式变更说明》。
- **新发现的「代码已改、文档未跟上」不一致（本轮核查重点）**：
  - **B1（高）**：`04_PRD_电商运营报表自动化Agent.md` 的 **NG4 明文写「角色不细分」**，与刚实现的 admin/user RBAC **直接矛盾** → 必须改写 NG4（原设计取舍 vs 任务书 §6.2 要求的冲突）。
  - **B3（高）**：`03_后端API与数据模型设计.md` 未同步 `User.role`、`/api/auth/me`、`?scope=all`。
  - B2：`05_UIUX设计文档.md` §2 用户画像仅「运营专员/运营主管」，缺 admin 角色。
  - B4：`项目说明_参数文档.md` §116 第 4 条「HTTPS 与数据库加密需部署阶段补齐」——HTTPS 已实现，该条过期。
  - B5：缺《指标验证方式变更说明》（任务书写"人工核算"基准，实际改为自动化双轨对账，需书面化）。
  - B6：§5.2 要求的「数据库设计文档 / API 接口文档」实际合并在 03 文档 §2/§3，需在交付清单标注边界。
- 收尾：C1 PPT 尚未体现 RBAC/HTTPS；C2 实训指导书与 RAG 语料待补新功能；C3 交付包压缩；C4 Golden 抽查；C5 答辩演练。
- 对账文档已更新（新增「七、执行进展更新」章节：已闭环 4 项 / 硬缺口 5 项 / 文档待同步 6 项 / 收尾 5 项 + 建议顺序）。
