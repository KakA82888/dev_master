# 剩余工作与下一步总报（PM · 关德豪 · 2026-09-08 11:09）

> 基于实测状态（非记忆），覆盖 D8 完成后全部剩余项。

## 一、已闭环（本会话内完成）
- S1–S5 代码层（提前 5–6 天）
- S6 评测：90 条评测集、SM1 三方差异 0.000000、SM2 100%（主集+留出集）、SM3 均值 0.06s
- S8 安全自查（八成）：guard 五类拦截 + SQLi 规则；缺前端拦截 UI 提示
- D8 LLM 通道：SiliconFlow `Pro/deepseek-ai/DeepSeek-R1` 接入，双通道对比 12/12 安全 + 15/15 解析，HTTP 端到端 14.2s 出报
- pytest 回归：26 passed（修复了 llm_client 误带 LLM_MODE 进测试进程的坑）

## 二、剩余内容清单

### A 类 · 需用户本人拍板（卡闭环）
| # | 事项 | 现状 | 建议 |
|---|---|---|---|
| D1 | Golden 复核 | 90 条标准答案全部 AI 预填（自验偏差，R1 风险） | 抽查 15 条（歧义5+边界5+注入5），约 20 分钟 |
| D3 | 数据源天池→UCI 变更 | 任务书 3.3 明文写天池，实际用 UCI，未书面化 | PM 出《数据源变更说明》待签认 |
| D4 | Git 真实身份 | 仍为占位 `ReportAgentDev <dev@example.com>` | 用户提供姓名+邮箱，改 config 并补规范提交 |

### B 类 · 0 进度交付硬货（文档线，答辩必需）
| # | 交付物 | 当前状态 | 说明 |
|---|---|---|---|
| 1 | `prompts/system` 系统提示词 | **0 文件** | 任务书第 3 项交付物 |
| 2 | `prompts/schema` / `templates` | **0 文件** | 第 3、4 项交付物；报告模板库 |
| 3 | `corpus/` 报表语料库 | **0 文件** | S9 交付 |
| 4 | `deliver/` 交付包 | **0 文件** | 最终打包 |
| 5 | S7 部署手册 + 一键启动脚本 | 0 进度 | 六类文档最后一块 |
| 6 | S9 AI 协作教程 | 0 进度 | 答辩硬交付 |
| 7 | S9 参数文档 | 0 进度 | 模型/阈值/口径说明 |
| 8 | S10 汇报 PPT | 0 进度 | 答辩主材 |

### C 类 · 收尾打磨
- 前端把"安全网关拦截"做成明确提示（非"生成失败"）
- 模型建议换 `DeepSeek-V3`（R1 做意图解析 9–52s 偏慢偏贵，无收益；改 `.env` 一行 `LLM_MODEL` 即可），本次按用户指定保留 R1
- Key 安全：用户 Key 曾在对话明文出现，建议 SiliconFlow 控制台轮换

## 三、建议推进顺序（待用户拍板后）
1. 用户回 D1/D3/D4 → PM 出《数据源变更说明》+ 拆文档
2. AI-Eng 落 `prompts/` 三件套 + 可选接 V3
3. BE 补 S7 部署手册 + 一键脚本
4. TW 做 S9 三件套 + PPT 大纲

## 四、实测证据速查
- `pytest -q` → 26 passed in 6.4s
- 后端 `http://127.0.0.1:8000/api/health` → `{"status":"ok"}`（LLM 通道存活）
- `find prompts -type f` → 0；`corpus/` → 0；`deliver/` → 0
- `.env` 存在（LLM 通道启用）；Git 身份仍为占位
- 评测产出：`data/eval/{reference_auto,parse_report,metrics_report,eval_set_proposed,eval_llm_report}.json`
